[project]
name = "bertalign"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "faiss-gpu",
    "jsonargparse",
    "numba",
    "numpy",
    "pandas",
    "spacy",
    "sentence-transformers",
    "ipython>=8.39.0",
]
[tool.uv]
package = true
